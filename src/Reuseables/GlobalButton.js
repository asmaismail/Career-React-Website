import React from 'react'

const GlobalButton = () => {
  return (
    <>
      {/* <button className={styleClass} onClick={click}>
        {title}
      </button> */}
    </>
  )
}

export default GlobalButton
